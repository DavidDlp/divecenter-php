{% extends 'base.html.twig' %}

{% block title %}Inmersiones{% endblock %}

{% block body %}
    <h1>Inmersiones</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inmersione.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ inmersione.name }}</td>
            </tr>
            <tr>
                <th>Lugar</th>
                <td>{{ inmersione.lugar }}</td>
            </tr>
            <tr>
                <th>Ciudad</th>
                <td>{{ inmersione.ciudad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inmersiones_index') }}">back to list</a>

    <a href="{{ path('inmersiones_edit', {'id': inmersione.id}) }}">edit</a>

    {{ include('inmersiones/_delete_form.html.twig') }}
{% endblock %}
