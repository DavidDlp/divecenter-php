<form method="post" action="{{ path('divecenter_delete', {'id': divecenter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ divecenter.id) }}">
    <button class="btn">Delete</button>
</form>
