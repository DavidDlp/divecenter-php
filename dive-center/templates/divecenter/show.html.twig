{% extends 'base.html.twig' %}

{% block title %}Divecenter{% endblock %}

{% block body %}
    <h1>Divecenter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ divecenter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ divecenter.name }}</td>
            </tr>
            <tr>
                <th>Ciudad</th>
                <td>{{ divecenter.ciudad }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ divecenter.direccion }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ divecenter.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('divecenter_index') }}">back to list</a>

    <a href="{{ path('divecenter_edit', {'id': divecenter.id}) }}">edit</a>

    {{ include('divecenter/_delete_form.html.twig') }}
{% endblock %}
